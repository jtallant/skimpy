{% extends 'layouts/master.twig' %}

{% block title %}{{ seotitle }}{% endblock title %}

{% block content %}
    <header>
        <h1>{{ title }}</h1>
        <p>Published: <time pubdate="pubdate">{{ date | date('F dS, Y') }}</time></p>
    </header>
    {{ content | raw }}
{% endblock content %}