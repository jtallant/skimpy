{% extends 'layouts/master.twig' %}

{% block header %}
    <h1 id="site-title"><a href="{{ url('home') }}">{{ app['site.title'] }}</a></h1>
    <p id="site-tagline">{{ app['site.tagline'] }}</p>
{% endblock header %}

{% block content %}
    {% for post in posts %}
        {% include 'partials/article.twig' %}
    {% endfor %}
{% endblock content %}