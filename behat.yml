default:
    autoload:
        '': %paths.base%/app/features/bootstrap
    suites:
        default:
            paths:
                - %paths.base%/app/features
    extensions:
        Behat\MinkExtension:
            base_url: http://localhost:4000
            goutte: ~