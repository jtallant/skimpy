default:
    autoload:
        '': %paths.base%/app/tests/features/bootstrap
    suites:
        default:
            paths:
                - %paths.base%/app/tests/features
    extensions:
        Behat\MinkExtension:
            base_url: http://localhost:4000
            goutte: ~